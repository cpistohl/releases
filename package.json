{
  "name": "releases",
  "devDependencies": {
    "@types/bootstrap": "^5.2.10",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "start": "bun run src/server/server.ts",
    "dev": "bun --hot src/server/server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@sentry/bun": "^10.39.0",
    "bootstrap": "^5.3.8"
  }
}
